import React, { useEffect } from 'react';

function Calendar() {
  useEffect(() => {
    // calendar.js에서 사용한 코드 구현 (예: 외부 라이브러리 사용)
    console.log('Calendar component loaded');
  }, []);

  return (
    <div id="calendar">
      {/* 실제 캘린더 컴포넌트를 여기에 구현하거나 외부 캘린더 라이브러리를 사용할 수 있습니다 */}
      <p>캘린더 영역</p>
    </div>
  );
}

export default Calendar;
